# Julia

## setup and run

Syntax

```
❯ julia src/words.jl -h
usage: words.jl [-s] [-h]

optional arguments:
  -s          Sort results
  -h, --help  show this help message and exit
```

Run

```
 julia -t10 src/words.jl
 ```
